<div class="col-12 offset-0 col-md-6 offset-md-0 col-lg-4 offset-lg-2 text-center">
    <div class="card bg-light mb-3" style="max-width: 35rem;">
        <div class="card-header"><strong>Fiches joueurs et encadrements</strong></div>
        <div class="card-body">
            <div class="row justify-content-center mt-2 mb-3">
                <div class="col-8">
                    <a href="{{ path('admin_staff_new') }}" class="btn btn-success" role="button">Nouveau joueur ou encadrant</a>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-6">
                    {{ form_start(formStaff) }}
                    {{ form_widget(formStaff) }}
                </div>
                <div class="col-1">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-edit"></i></button>
                </div>
                {{ form_end(formStaff) }}
            </div>
        </div>
    </div>
</div>