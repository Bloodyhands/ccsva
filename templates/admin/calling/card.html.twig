<div class="col-12 col-md-6 col-lg-4 text-center">
    <div class="card bg-light mb-3" style="max-width: 35rem;">
        <div class="card-header"><strong>Convocation</strong></div>
        <div class="card-body">
            <div class="row justify-content-center mt-2 mb-3">
                <div class="col-8">
                    <a href="{{ path('admin_calling_new') }}" class="btn btn-success" role="button">Nouvelle convocation</a>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-6">
                    {{ form_start(formCalling) }}
                    {{ form_widget(formCalling) }}
                </div>
                <div class="col-1">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-edit"></i></button>
                </div>
                {{ form_end(formCalling) }}
            </div>
        </div>
    </div>
</div>