{% extends 'base.html.twig' %}

{% block title %}
{% endblock %}

{% block body %}

    <div class="row justify-content-center">
        <h5>Les joueurs et encadrants</h5>
    </div>

    {% for staff in staff %}

        <div class="row justify-content-center">
            <div class="card text-white bg-primary mb-3" style="max-width: 40rem;">
                <div class="card-body">
                    <div class="row">
                        <dt class="col-12 col-md-6">Nom/Prénom :</dt>
                        <dd class="col-12 col-md-6">{{ staff.name|capitalize }} {{ staff.firstname|capitalize }}</dd>
                    </div>
                </div>
            </div>
        </div>

    {% endfor %}

{% endblock %}